{
  "name": "@invoice-generator/pdf-generator",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf node_modules .turbo dist",
    "clean:cache": "rm -rf .turbo"
  },
  "dependencies": {
    "@invoice-generator/shared-types": "workspace:*",
    "@react-pdf/renderer": "^4.1.5",
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "react": "^19.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  }
}
