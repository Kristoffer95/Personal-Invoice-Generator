## auto-height-table-propagation (Bug Fix) - COMPLETED

### Task completed
PRD: auto-height-table-propagation
Tables (Work Hours, Line Items, Summary) now cause parent Row/Column containers to expand when heightMode='auto'.

### Key decisions
- Added TableContentData interface to pass row counts to layout engine
- Created estimateTableHeight() using constants: HEADER=24pt, ROW=22pt, MIN=40pt
- Modified calculateAutoHeight() to include table height estimates via isTableElement() check
- Propagated tableContent through all layout functions: calculateEffectiveHeight, getFlexBasis, calculateContainerChildPositions, calculateGridChildPositions, processContainer, calculateElementPositions
- PDF generator builds TableContentData from invoice (workDays, lineItems, discount/tax rows)
- Style Editor uses sample data (3 work rows, 2 line items, 4 summary rows)

### Files changed
- packages/shared-types/src/layout-engine.ts: Added TableContentData interface, estimateTableHeight(), isTableElement(), updated all height calculation functions
- packages/pdf-generator/src/TemplatePDF.tsx: Build and pass TableContentData from invoice
- apps/web/src/components/style-editor/StyleEditorCanvas.tsx: Pass sample TableContentData for preview

### Notes
- Build passes. Tests fail due to pre-existing rollup arm64 module issue, lint fails due to missing eslint config in shared-types - both unrelated to this change.
- Table height estimation is approximate (header + rows * rowHeight). Actual PDF rendering may vary slightly but should be close enough for layout purposes.
