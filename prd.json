{
  "convex-schema-sizing-modes": {
    "category": "Bug Fix",
    "description": "Add missing sizing mode fields (widthMode, widthPercent, heightSizingMode, heightSizingPercent, flexBasis, gridColumn, gridRow) to Convex templateElementValidator in both schema.ts and templates.ts to match the Zod schemas and frontend save handler. Currently, saving templates with sizing mode changes fails with ArgumentValidationError.",
    "stepsToVerify": [
      "Open the Style Editor with an existing template",
      "Select any element (e.g., Work Hours Table)",
      "Modify the element's sizing mode via the Properties Panel (change width or height sizing)",
      "Click Save in the Style Editor header",
      "Verify no console errors appear (specifically no ArgumentValidationError)",
      "Refresh the page and confirm the template loads with the saved changes",
      "Create a new element, adjust its sizing settings, and save again to verify new elements work too"
    ],
    "passes": true
  }
}
